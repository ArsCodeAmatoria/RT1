/**
 * Content source adapter.
 *
 * Today: local TypeScript seed modules under `content/`.
 * Later: replace these imports with Sanity GROQ / Payload Local API
 * while keeping the same function signatures in `lib/content`.
 */
export {
  siteConfig,
  mainNavigation,
  footerNavigation,
  platformNavigation,
  homePage,
  projects,
  projectsById,
  servicesIndexPage,
  projectsPage,
  teamPage,
  careersPage,
  contactPage,
  equipmentPage,
  reinforcingPage,
  servicesRegistry,
  serviceList,
} from "@/content";
